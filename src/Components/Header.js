import React from "react";
import Logout from "./Logout";

const Header = () => {
  return (
    <header>
      <h1>Mini Dogs</h1>
      <Logout />
    </header>
  );
};

export default Header;
